apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: maxprime
spec:
  runLatest:
    configuration:
      revisionTemplate:
        spec:
          # containerConcurrency: 1
          container:
            image: gcr.io/s9-demo/maxprime:latest
            env:
            - name: RELEASE
              value: "v0.0.2"